@model IEnumerable<Implementacija.Models.RezervacijaDvorane>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.rezervacija)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.izvodjac)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.dvorana)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.rezervacija.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.izvodjac.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.dvorana.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
